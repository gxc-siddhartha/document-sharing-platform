class SharedPreferencesKeys {
  // Renamed for clarity and better alignment with common practice
  // Key for storing authentication status (true if logged in, false otherwise)
  static const String isAuthenticated = 'is_authenticated';

  // Key for storing the ID of the currently logged-in user (if any)
  static const String userId = 'user_id'; // More descriptive name
}
